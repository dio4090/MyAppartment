------DATABASE PROJECT INFORMATION------


USER:
-- Get last user from database;
User u = new User();
DBUser dbUser = new DBUser();

//Implement in method
user = getUserFromDB(u);

//User to ger user information
User getUserFromDB(User u) {
    try {
        dbUser.open();
        u = dbUser.retreiveUserFromDB(u);
        dbUser.close();
        return u;
    } catch (Exception e) {
        Log.e(TAG, "<loadImageFromDB> Error : " + e.getLocalizedMessage());
        dbUser.close();
        return null;
    }
}



SELECT WHERE SPECIFIC VALUE
    public User getLastLoggedUser() {
//        String[] whereArgs = new String[] {
//                ""
//                //"value2"
//        };

        String selectUser =
                "SELECT "+EMAIL+","+PASSWORD+" FROM "+USER_TABLE +" " +
                        "WHERE email = ? ";

        Cursor cursor = mDb.rawQuery(selectUser, whereArgs);

//        if (cursor.moveToFirst()){
//            do {
//                // Passing values
//                UserSessionData.NAME = (cursor.getString(cursor.getColumnIndex("name")));
//                UserSessionData.ADDRESS = (cursor.getString(cursor.getColumnIndex("address")));
//                UserSessionData.EMAIL = (cursor.getString(cursor.getColumnIndex("email")));
//                UserSessionData.MOBILE_NUMBER = (cursor.getInt(cursor.getColumnIndex("mobile_number")));
//            } while(cursor.moveToNext());
//        }

        cursor.close();
        mDb.close();
    }